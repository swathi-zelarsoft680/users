@Library('todoshop') _

todoshop (
        COMPONENT             : 'users',
        PROJECT_NAME          : "todoshop",
        SLAVE_LABEL           : "JAVA",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "JAVA"
)
